# --- Local laptop only ---
# NEXT_PUBLIC_API_URL=http://localhost:8000

# --- Phone testing via ngrok (update both URLs when tunnels restart) ---
NEXT_PUBLIC_API_URL=https://YOUR-BACKEND-ID.ngrok-free.app
NGROK_FE_HOST=YOUR-FRONTEND-ID.ngrok-free.app

# Backend surecap_dossier_search/.env needs: CORS_ALLOW_ALL=true
# Restart API + npm run dev after any env change.
